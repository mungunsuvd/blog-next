import { Button } from "react-bootstrap";
import React from "react";
const Buttonnn = ({ className, value, onclick }) => (
  <button className={className}>{value}</button>
);
export default Buttonnn;
